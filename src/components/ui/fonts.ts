import { Lusitana, Fugaz_One } from 'next/font/google'

export const fugazOne = Fugaz_One({subsets: ['latin'], weight: "400", display: 'swap'})
export const lusitana = Lusitana({subsets: ['latin'], weight: "400", display: 'swap'})
